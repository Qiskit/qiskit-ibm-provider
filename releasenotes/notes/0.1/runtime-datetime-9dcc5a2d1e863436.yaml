---
upgrade:
  - |
    :class:`qiskit_ibm.runtime.utils.RuntimeEncoder` and
    :class:`qiskit_ibm.runtime.utils.RuntimeDecoder`
    are updated to support Python ``datetime``, which is not
    JSON serializable by default.
