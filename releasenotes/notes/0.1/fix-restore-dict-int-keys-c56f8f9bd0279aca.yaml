---
fixes:
  - |
    :class:`qiskit_ibm.runtime.RuntimeDecoder` can now restore dictionary integer keys
    in optimizer settings from a JSON string representation dumped by the
    :class:`qiskit_ibm.runtime.RuntimeEncoder`.
