---
features:
  - |
    Runtime image can now be specified using the `image` parameter in
    :meth:`qiskit_ibm.runtime.IBMRuntimeService.run`.
    Note: not all accounts are authorized to select a different image.
