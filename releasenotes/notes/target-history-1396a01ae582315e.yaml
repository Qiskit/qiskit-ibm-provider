---
features:
  - |
    Added the method :meth:`~qiskit_ibm_provider.IBMBackend.target_history`. 
    This method is similar to :meth:`~qiskit_ibm_provider.IBMBackend.target`.
    The difference is that the new method enables the user to pass a `datetime` parameter,
    to retrieve historical data from the backend.

